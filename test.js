var level = 3;
var angka = 1;

for (var baris = 0; baris < level; baris++) 
{
    var result = '';
    for (var kolom = 0; kolom < level; kolom++ ) 
    {
        result = resut + angka + 'x';
        angka++;
    }
  console.log (xo);
} 
